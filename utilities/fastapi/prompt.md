напиши python fastapi сервис 

который принимает json вида:

route запроса: quote_gun/add

скрипт добавит в mongodb на сервере в базу quote_gun коллекцию quotes этот джисон как документ

я хочу сделать один скрипт fast api описывающий много запросов которые будут вызывать другие скрипты и выполнять разные операции

разбей скрипт на файлы

допиши статический токен хранящийсяв .env файле чтобы защитить от стороннего вмешательства
